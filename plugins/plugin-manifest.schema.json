{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://finregguard.local/plugin-manifest.schema.json",
  "title": "RegGuard Plugin Manifest",
  "description": "Canonical registry of every capability the application exposes. The manifest is the single source of truth for what the app can do; the audit script verifies the manifest against the codebase and reports drift.",
  "type": "object",
  "required": ["version", "plugins"],
  "additionalProperties": false,
  "properties": {
    "version": {
      "type": "string",
      "description": "Manifest schema version (semver). Bump on breaking schema changes.",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp of last manifest edit."
    },
    "plugins": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/Plugin" }
    }
  },
  "$defs": {
    "Plugin": {
      "type": "object",
      "required": ["key", "name", "category", "status"],
      "additionalProperties": false,
      "properties": {
        "key": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$",
          "description": "Stable identifier. Must match the ViewKey / route segment used in code."
        },
        "name": {
          "type": "string",
          "minLength": 2,
          "description": "Human-readable name shown in UI."
        },
        "description": {
          "type": "string",
          "description": "One-line explanation of what this plugin does."
        },
        "category": {
          "type": "string",
          "enum": [
            "core-compliance",
            "surveillance",
            "quant-computational",
            "intelligence-automation",
            "collaboration-trust",
            "platform-governance",
            "infrastructure",
            "data-connector"
          ],
          "description": "Top-level grouping. Should match nav zone structure."
        },
        "status": {
          "type": "string",
          "enum": ["ready", "in_progress", "planned", "deprecated", "not_implemented"],
          "description": "Explicit absence state. ready = fully working and audited; in_progress = file exists, partially implemented; planned = manifest entry only, no code; deprecated = explicitly retired; not_implemented = no code and no current plan to build (shows 404-style page)."
        },
        "owner": {
          "type": "string",
          "description": "Person or team responsible. Optional but recommended."
        },
        "expectedDelivery": {
          "type": "string",
          "format": "date",
          "description": "For in_progress / planned only. ISO date."
        },
        "implementation": {
          "type": "object",
          "additionalProperties": false,
          "description": "References to where this plugin lives in the codebase. Used by the audit script to detect drift.",
          "properties": {
            "component": {
              "type": "string",
              "description": "Path to the React component file, relative to repo root. e.g. src/components/forms/FormInstanceView.tsx"
            },
            "viewKey": {
              "type": "string",
              "description": "The ViewKey string used in page.tsx switch statement, if this is a UI plugin."
            },
            "apiRoute": {
              "type": "string",
              "description": "API route path, e.g. /api/forms. Used to verify the endpoint exists."
            },
            "staticData": {
              "type": "string",
              "description": "Path to static JSON data file used in static-build mode, e.g. public/data/forms/instances.json"
            }
          }
        },
        "capabilities": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Granular capability list. Each entry is individually auditable. e.g. ['create-instance', 'run-autofill', 'view-provenance']"
        },
        "dynamism": {
          "type": "object",
          "additionalProperties": false,
          "description": "Declares the static/dynamic capacity profile for this plugin. Implements the 'reserved buffer' pattern: the static build ships the full data shape with nullable slots for fields that will eventually be filled by a live API. The runtime tries the API, falls back to static, merges on success. See STATIC-DYNAMIC-CAPACITY-PATTERN.txt for the full rationale.",
          "properties": {
            "level": {
              "type": "string",
              "enum": ["L0", "L1", "L2", "L3"],
              "description": "Certainty level of dynamism. L0 = pure static (regulation text, framework definitions). L1 = static + periodic refresh (regulation status, KPI counts). L2 = static shell + dynamic data (form instances, audit events). L3 = fully dynamic (AI chat, real-time surveillance)."
            },
            "staticShell": {
              "type": "object",
              "description": "Which top-level fields of the static JSON are 'full' (rendered immediately) vs 'stub' (placeholder, will be replaced by dynamic data). The audit verifies that stub fields actually exist as nullable in the static JSON.",
              "additionalProperties": { "type": "string", "enum": ["full", "stub", "absent"] }
            },
            "dynamicSlots": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Field paths (dotted notation, e.g. 'summary.realTimePassRate') that are reserved for dynamic population. Must be present as null in the static JSON. The audit verifies this."
            },
            "refreshStrategy": {
              "type": "string",
              "enum": ["on-mount", "on-focus", "interval", "manual", "never"],
              "description": "When the client should attempt to fetch dynamic data. on-mount = once on page load; on-focus = re-fetch on window focus; interval = poll on a timer; manual = user action only; never = static only."
            },
            "refreshIntervalMs": {
              "type": "integer",
              "minimum": 1000,
              "description": "For refreshStrategy=interval only. Polling interval in milliseconds."
            },
            "fallback": {
              "type": "string",
              "enum": ["static-json", "empty-state", "error"],
              "description": "What to render if the dynamic fetch fails. static-json = keep showing the static values; empty-state = show a 'data unavailable' message; error = throw."
            },
            "timeoutMs": {
              "type": "integer",
              "minimum": 100,
              "description": "How long to wait for the dynamic fetch before falling back. Default 2000."
            }
          },
          "required": ["level"]
        },
        "audit": {
          "type": "object",
          "additionalProperties": false,
          "description": "Per-plugin verification criteria. The audit script uses these to determine pass/fail.",
          "properties": {
            "minContentLength": {
              "type": "integer",
              "minimum": 0,
              "description": "Minimum rendered text length (chars) expected on the page. Default 200."
            },
            "expectedHeading": {
              "type": "string",
              "description": "Substring expected in the page's main heading. Case-insensitive."
            },
            "requiresLiveRuntime": {
              "type": "boolean",
              "description": "If true, this plugin only fully works on the dev server (needs DB / API). Static build will show a degraded but non-empty state. Default false."
            },
            "requiresBackButton": {
              "type": "boolean",
              "description": "If true, the audit verifies a Back button is present on this page. Default true for all non-dashboard pages."
            }
          }
        },
        "dependencies": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Keys of other plugins this one depends on. Audit will warn if a dependency is not ready."
        }
      }
    }
  }
}
